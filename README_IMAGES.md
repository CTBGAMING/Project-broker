Important: This environment could not fetch external images to embed directly into the ZIP.
Run the script `bash scripts/download_luxury_images.sh` locally (or open it and download images manually) to populate `src/assets/` with luxury-themed hero images.
The project will reference `src/assets/hero_luxury_1.jpg` etc. Once downloaded, rebuild the site.
